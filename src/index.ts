import {
  scanHandler,
  getWordByIdHandler,
  addWordHandler,
} from "./lambdaHandlers";
import { botHandler } from "./bot";

export { botHandler, scanHandler, getWordByIdHandler, addWordHandler };
